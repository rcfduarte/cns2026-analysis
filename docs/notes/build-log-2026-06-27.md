# Corpus build log — 2026-06-27

First end-to-end build of the CNS abstract corpus (Plan 1 foundation).

## Pipeline status

Tasks 1–7 complete, 12 offline tests green, ruff clean. Live acquisition validated
for Era A (PMC individual-DOI) and Era C (JCN flat-paragraph bundle).

## Era A (2007–2015, BMC individual DOIs) — built

`python -m cns_scientometrics build --years 2007-2015 --out data/corpus --no-enrich`

| Year | Abstracts | Note |
|------|-----------|------|
| 2007 | 234 | OK (vol 8 **S2**) |
| 2008 | 17  | ⚠ PMC gap — see below |
| 2009 | 1   | ⚠ PMC gap — see below |
| 2010 | 218 | OK |
| 2011 | 409 | OK |
| 2012 | 212 | OK |
| 2013 | 460 | OK |
| 2014 | 241 | OK |
| 2015 | 323 | OK |

Total 2115 abstracts; missing-title 0.0, missing-body 0.0. Types: 3 keynote / 145 oral
/ 1967 poster. (`oral` includes featured `F#` talks; classification by DOI code prefix.)

### Key fix during build
`esearch "BMC Neurosci"[Journal] AND <year>[PDAT]` returns the **whole journal**, not
just the CNS supplement — regular research articles (DOI `…/s12868-…`) were polluting
the corpus and collapsing to a `P0` fallback id (100 false dupes in 2015). Fix: keep a
record only if its DOI matches the supplement pattern `-S<n>-<CODE>` (`is_supplement_abstract`),
and broaden code prefixes beyond `[OPA]` to capture featured/keynote (`F`, `K`, `I`, `T`, `W`).
After the fix: 0 dupes in 2015, clean type split.

### ⚠ 2008 / 2009 PMC gap (data availability, not a bug)
CNS*2008 (vol 9 S1) and CNS*2009 (vol 10 S1) individual abstracts are **not deposited in
PMC** as separate articles: `10.1186/1471-2202-{9,10}-S1-P1[DOI]` resolves to nothing, and
`<year>[PDAT]` returns only other BMC supplements (S2/S3) plus regular articles. These two
years must be recovered from the **BMC supplement landing pages** (HTML) if needed:
- 2008: https://bmcneurosci.biomedcentral.com/articles/supplements/volume-9-supplement-1
- 2009: https://bmcneurosci.biomedcentral.com/articles/supplements/volume-10-supplement-1

Deferred — a small HTML-scrape fetcher would close the gap; out of scope for the PMC-based
foundation.

## Era C (2021, JCN bundle) — validated
`--years 2021-2021` → 237 abstracts (4 keynote / 20 oral / 213 poster), body length
median 2579 chars, 0 tiny bodies. JCN bundle is a **flat `<body><p>` stream** (no `<sec>`);
segmented on header paragraphs (`K1`, `F1`, `O5`, `P173`). Bundle = PMC8687879.

## Remaining acquisition work (for Plan 1 completion)

1. **Era B (2016–2020, BMC bundled "Part 1/2/3")** — resolve the Part-article PMCIDs per
   year and verify whether they are `<sec>`-structured (use `split_bundle`) or flat
   (`split_flat_paragraphs`); the router already falls back flat if no `<sec>` found.
2. **Era C 2022–2025** — the JCN supplement DOIs from the scoping research
   (`10.1007/s10827-022-00841-9`, `-024-00871-5`, `-024-00889-9`) do **not** resolve in
   PMC by DOI. Verify each against the live Springer/PMC page; some JCN supplements may be
   Springer-only (use the Springer Nature API / publisher JATS path rather than `efetch`).
3. **2008/2009 BMC HTML fallback** (optional) to close the Era A gap.

## Corpus location
`data/corpus/` (gitignored). Era C text is `free-to-read`, not CC-BY — do not redistribute
raw body text for 2021+.
